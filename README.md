# KankorPrep Afghanistan

Mobile-first Kankor preparation platform.

Phase 1 establishes the mobile app shell, RTL/localization architecture, shared design system, backend foundation, and PostgreSQL/Drizzle database foundation.

After pulling the repository, install dependencies with `npm install` and use the workspace scripts in `package.json`.
